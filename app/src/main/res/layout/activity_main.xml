<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context="de.uni_stuttgart.informatik.sopra.sopraapp.CockpitMainActivity"
    tools:openDrawer="start">

    <include
        layout="@layout/activity_main_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <android.support.design.widget.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="320dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        android:layout_marginTop="0dp"
        android:paddingTop="0dp"
        android:theme="@style/NavigationTheme"
        android:background="@color/drawerBackgroundColor"
        app:elevation="16dp"
        app:headerLayout="@layout/drawer_header"
        app:itemTextColor="@color/colorPrimaryDark"
        app:itemIconTint="@color/colorPrimaryDark"
        app:insetForeground="@color/colorPrimaryTextLight"
        app:menu="@menu/main_menu" />

</android.support.v4.widget.DrawerLayout>
